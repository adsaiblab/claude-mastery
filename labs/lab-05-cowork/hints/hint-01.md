# Hint 01 — Lab 05

Ne skippe pas la baseline. C'est le point de référence — sans elle, pas de "plus haut que". Un one-shot rapide (sans plan, sans review) suffit.

Pour la baseline : lance `claude -p "Implémente la spec de rate-limit dans ce module..."` et accepte le premier résultat cohérent. Ne re-itère pas.

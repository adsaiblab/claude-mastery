---
title: Référence — Slash commands
description: Toutes les slash-commands natives et comment créer les tiennes.
sidebar:
  order: 1
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="3" blocs={["Bloc 9 — Slash commands"]}>
  Tableau exhaustif des slash-commands natives (`/help`, `/clear`, `/compact`, `/fork`, `/resume`, `/model`, `/permissions`, `/hooks`, `/agents`, `/mcp`, `/plugin`, `/login`, `/logout`, `/rename`, `/branch`, `/cleanup-team`, `/skill`, etc.) + format d'une commande custom (frontmatter `description`, `allowed-tools`, `model`, body en prompt templatisé avec `$ARGUMENTS`).
</Planned>

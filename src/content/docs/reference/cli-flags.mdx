---
title: Référence — CLI flags
description: Tous les flags de la commande `claude` avec usage et défauts.
sidebar:
  order: 2
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="3" blocs={["Bloc 7 — CLI flags"]}>
  Tableau exhaustif : `-p` / `--print`, `-c` / `--continue`, `--resume`, `--fork-session`, `-n` / `--name`, `--model`, `--fallback-model`, `--permission-mode`, `--allowed-tools`, `--disallowed-tools`, `--add-dir`, `--mcp-config`, `--mcp-debug`, `--agent`, `--dangerously-skip-permissions`, `--output-format`, `--verbose`, `--debug`, `--session-id`, etc.
</Planned>

---
title: Référence — Frontmatter (subagents, skills, commands)
description: Tous les champs YAML acceptés dans les fichiers .md de config.
sidebar:
  order: 5
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="4" blocs={["Bloc 12 — Subagents", "Bloc 14 — Skills"]}>
  Tableau par type : **subagent** (`name`, `description`, `model`, `tools`, `permissionMode`, `maxTurns`, `skills`, `mcpServers`, `hooks`, `memory`, `background`, `effort`, `isolation`, `color`, `initialPrompt`), **skill** (`name`, `description`, `allowed-tools`, `model`), **slash-command** (`description`, `allowed-tools`, `model`, `argument-hint`).
</Planned>

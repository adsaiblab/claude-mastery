---
title: Référence — settings.json
description: Toutes les clés acceptées dans .claude/settings.json et ~/.claude/settings.json.
sidebar:
  order: 6
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="3" blocs={["Bloc 10 — Permissions", "Bloc 11 — Hooks"]}>
  Clés : `permissions` (`allow`, `deny`, `ask`, `defaultMode`, `additionalDirectories`), `hooks`, `env`, `mcpServers`, `model`, `cleanupPeriodDays`, `includeCoAuthoredBy`, `statusLine`, `outputStyle`, `enableAllProjectMcpServers`, priorité de fusion user/project/local.
</Planned>

---
title: Pattern — Context Engineering
description: Protéger le context principal via subagents, compression et stratification.
sidebar:
  order: 4
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="4" blocs={["Bloc 27 — Patterns avancés"]}>
  Diagramme, techniques : (1) subagent "researcher" qui renvoie un résumé court, (2) `/compact` périodique, (3) mémoire typée (MEMORY.md index + fichiers thématiques), (4) fork pour explorer sans polluer, (5) stratification system > CLAUDE.md > memory > files > chat. Anti-patterns : tout dans CLAUDE.md, pas de résumé entre phases, contexte qui explose silencieusement.
</Planned>

---
title: GitHub Actions
description: Claude Code en CI — issue triage, PR review, auto-fix.
sidebar:
  order: 2
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="5" blocs={["Bloc 18 — GitHub Actions"]}>
  Action `anthropics/claude-code-action`, inputs (`prompt`, `allowed_tools`, `model`, `timeout_minutes`), secrets (`ANTHROPIC_API_KEY`), permissions GITHUB_TOKEN, trigger patterns (`issue_comment`, `pull_request`, `workflow_dispatch`), exemples : PR reviewer, issue triager, auto-fix typos.
</Planned>

---
title: Agent SDK — TypeScript
description: Version Node du SDK — streaming, tool use, intégration backends JS.
sidebar:
  order: 4
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="5" blocs={["Bloc 20 — Agent SDK TypeScript"]}>
  Package `@anthropic-ai/claude-agent-sdk`, `query()` avec `AsyncIterable`, options identiques à Python, typage complet Zod/TS, intégration Next.js API routes et Express, gestion backpressure streaming, tests avec mocks.
</Planned>

---
title: Agent SDK — Python
description: Lancer Claude Code programmatiquement depuis Python (FastAPI, scripts, workers).
sidebar:
  order: 3
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="5" blocs={["Bloc 19 — Agent SDK Python"]}>
  Package `claude-agent-sdk`, `query()` async, `ClaudeAgentOptions` (`system_prompt`, `allowed_tools`, `permission_mode`, `model`, `max_turns`, `cwd`), streaming des messages (`AssistantMessage`, `ToolUseBlock`, `ResultMessage`), gestion des erreurs, intégration FastAPI (exemple endpoint `/analyze`), différence avec l'API Anthropic brute.
</Planned>

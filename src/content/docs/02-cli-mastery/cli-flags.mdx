---
title: CLI flags
description: Toutes les flags de claude — flip cards interactives filtrables par catégorie.
sidebar:
  order: 2
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="3" blocs={["Bloc 10 — CLI flags référence", "Bloc 16 — CLI flags exhaustif"]}>
  ~80 flags groupées en 10 catégories (session, modèle, permissions, tools, MCP, plugins, output, remote, auth, debug). FlipCards filtrables, lien direct vers la `Référence/cli-flags` complète.
</Planned>

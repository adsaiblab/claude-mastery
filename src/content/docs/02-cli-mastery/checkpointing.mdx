---
title: Checkpointing
description: Esc+Esc, /rewind, restore code/conversation — le filet de sécurité qui permet d'oser.
sidebar:
  order: 4
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="3" blocs={["Bloc 7 — Checkpointing"]}>
  Scénario "refactoring 15 fichiers → Claude dérive → rewind à mi-parcours". 5 actions du menu rewind (restore code+conv, code only, conv only, summarize, cancel). API SDK `rewind_files()` / `rewindFiles()`. Flag `CLAUDE_CODE_DISABLE_FILE_CHECKPOINTING`.
</Planned>

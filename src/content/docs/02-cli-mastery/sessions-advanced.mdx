---
title: Sessions avancées
description: Continue, resume, fork, naming, tags — naviguer dans ton historique de conversations.
sidebar:
  order: 7
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="3" blocs={["Bloc 8 — Sessions"]}>
  `claude -c`, `--resume`, `--fork-session`, `-n`, `/rename`, `/branch`, interaction avec git branches, pièges (même session-id dans 2 terminaux entrelace les JSONL), `listSessions` / `tagSession` via SDK.
</Planned>

---
title: Plugins
description: Marketplace Claude Code — installation, création, distribution de bundles.
sidebar:
  order: 6
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="4" blocs={["Bloc 16 — Plugins"]}>
  Structure d'un plugin (`.claude-plugin/plugin.json`, agents/, skills/, commands/, hooks/), `claude plugin install <name>`, marketplace officiel vs marketplaces tiers, packaging (skills + subagents + slash-commands + hooks ensemble), versioning, sécurité (review du code avant install).
</Planned>

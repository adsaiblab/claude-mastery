---
title: MCP — Model Context Protocol
description: Connecter des serveurs d'outils externes (GitHub, Slack, Linear, bases de données).
sidebar:
  order: 5
draft: false
---

import Planned from '../../../components/Planned.astro';

<Planned session="4" blocs={["Bloc 15 — MCP"]}>
  Architecture MCP (client/serveur/transports stdio/SSE/HTTP), `claude mcp add` / `claude mcp list` / `claude mcp remove`, scopes (user/project/local), config JSON (`mcpServers`), serveurs officiels (`@modelcontextprotocol/server-filesystem`, `server-github`, `server-slack`), auth OAuth vs token, debugging avec `--mcp-debug`.
</Planned>
